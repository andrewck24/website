## 1. Sanity Schema and Query

- [x] 1.1 Add `resumePdfZh` and `resumePdfEn` optional `file` fields (`.pdf` only) to the `about` schema in `schemas/about.ts`. Verify: both fields appear in Sanity Studio's about document editor after local `pnpm sanity dev` startup. Satisfies: Sanity resume PDF fields requirement.

- [x] 1.2 Update `getAboutQuery` in `src/lib/sanity/queries.ts` to project `"resumePdfZhUrl": resumePdfZh.asset->url` and `"resumePdfEnUrl": resumePdfEn.asset->url`. Verify: a TypeScript type for the query result includes `resumePdfZhUrl: string | null` and `resumePdfEnUrl: string | null`; `pnpm type-check` passes. Satisfies: Sanity resume PDF fields requirement; implements the Sanity two-field approach for resume PDFs design decision.

## 2. Page Wiring

- [x] 2.1 Update `src/app/(site)/[lang]/about/[[...slug]]/page.tsx`: remove `lg:flex-row` and `lg:items-start lg:justify-center` from the page wrapper; replace `<PersonalInfo />` with `<BusinessCard lang={lang} pdfUrls={{ zh: about.resumePdfZhUrl, en: about.resumePdfEnUrl }} />`; on the `<article>` element remove `border border-border rounded-2xl px-4 py-12 lg:px-8` and replace with `px-0 py-6 lg:px-8 lg:py-12`. Verify: BusinessCard renders above article on all viewport widths; no sticky aside at lg+; article content spans full width on mobile with no visible border; `pnpm type-check` passes. Satisfies: BusinessCard above-article layout; implements About page layout: single-column on all breakpoints and Article styling: remove border and reduce mobile padding design decisions.

## 3. Component Creation

- [x] [P] 3.1 Create `src/components/about/business-card.tsx` as a client component (`'use client'`). The gradient card (`from-gradient-3 to-gradient-5 bg-linear-to-br`) SHALL contain the owner name `<h1>`, job title `<p>`, and all three social link buttons (GitHub, LinkedIn, Cake) in a bottom row inside the card. Accepts `lang: 'zh-TW' | 'en' | 'ja'` and `pdfUrls: { zh: string | null; en: string | null }` props. When `pdfUrls.zh === null && pdfUrls.en === null`, the PDF trigger button is absent. Verify: at `/[lang]/about`, all social buttons appear inside the gradient card; `pnpm type-check` passes. Satisfies: BusinessCard above-article layout requirement.

- [x] [P] 3.2 Create `src/components/about/resume-dialog.tsx` as a client component. Uses shadcn/ui `Dialog`. Renders a download link for `pdfUrls.zh` (label from i18n table: 中文 / Traditional Chinese / 台湾華語) only when non-null; same for `pdfUrls.en` (英文 / English / 英語). Dialog title and description text use the i18n table defined in the design doc (下載履歷 PDF / Download Resume PDF / 履歴書PDFをダウンロード; 選擇語言版本 / Choose your preferred language / 言語を選択してください). Each link uses `target="_blank" rel="noopener noreferrer"`. Accepts `lang`, `pdfUrls`, and `defaultOpen: boolean` props. Verify: in browser with both URLs set, dialog shows two links; with one URL null, only one link shows; `pnpm type-check` passes. Satisfies: Resume PDF download dialog requirement.

## 4. Collapsed Sticky Header

- [x] 4.1 Add IntersectionObserver to `BusinessCard`: place a `<div data-sentinel className="h-0" />` as the **first child** of the BusinessCard wrapper div. In `useEffect`, read `document.querySelector('nav')?.getBoundingClientRect().bottom ?? 72` as `navBottom`, then create an `IntersectionObserver` with `{ rootMargin: '-${navBottom}px 0px 0px 0px', threshold: 0 }` observing the sentinel. When `isIntersecting` is false, set `data-collapsed="true"` on the wrapper; when true, remove the attribute. Verify: browser DevTools shows `data-collapsed="true"` on the wrapper element when the card top reaches the navbar bottom; attribute is removed when scrolled back to top. Satisfies: collapsed header: IntersectionObserver over CSS scroll-driven animations design decision.

- [x] 4.2 Add CSS styles for collapsed state to `BusinessCard`. When `data-collapsed` is present: card wrapper becomes `position: sticky; top: calc(var(--navbar-top-gap) + var(--navbar-height)); z-index: 50`; on lg+ viewport, transitions to a single-row flex layout showing `h1` (name) and `p` (title) alongside all social buttons; on viewports < lg, shows `h1` only with `p` and all buttons hidden. Use `transition-all duration-200` for opacity and sizing changes. Verify: manually scroll on lg+ → name + title + buttons appear as a sticky single row directly below the navbar; on mobile → name only; scroll back up → full card restores. Satisfies: BusinessCard collapsed sticky header requirement.

- [x] 4.3 Add click-to-top behavior to `BusinessCard`: when `data-collapsed` is true, clicking the card calls `window.scrollTo({ top: 0, behavior: 'smooth' })` and applies `cursor-pointer` to the card; no click handler when expanded. Verify: in browser, click collapsed card → page scrolls to top; click expanded card → no effect. Satisfies: collapsed card click-to-top requirement.

## 5. Shareable Resume URL

- [x] 5.1 Create a `ResumeDialogWithParams` client wrapper that calls `useSearchParams()` and derives `defaultOpen = searchParams.get('resume') === 'open'`; passes `defaultOpen` to `ResumeDialog`; on dialog close when `defaultOpen` was true, calls `router.replace(pathname, { scroll: false })`. Wrap this component in `<Suspense fallback={<ResumeTriggerSkeleton />}>` where `ResumeTriggerSkeleton` is a button-shaped skeleton placeholder (matching the trigger button's size). Wrap the `<Suspense>` in a React `ErrorBoundary` that renders `null` on error. Verify: SSR HTML contains the skeleton placeholder (visible before hydration); navigating to `/[lang]/about?resume=open` opens dialog immediately after hydration; closing results in URL `/[lang]/about`; pressing Back goes to the previous page; opening via trigger does not change URL; `pnpm build` passes (static generation preserved). Satisfies: Shareable resume dialog URL requirement; implements searchParams: client useSearchParams with Suspense over server searchParams prop design decision.
