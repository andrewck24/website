## 1. Surface-color token values (two-surface-role-model, card-elevation-contrast)

- [x] 1.1 Implement the design's "two-role surface model with elevation" decision by updating `:root` and `.dark` in `src/app/globals.css` so `--background` resolves to `#fafafa` (light) / `#080808` (dark) and `--card`/`--popover` resolve to `#ffffff` (light) / `#171717` (dark) — satisfying the two-surface-role-model and card-elevation-contrast requirements (card always perceptibly lighter than the page in both modes). Verify with `grep -E "^\s*--(background|card|popover):" src/app/globals.css` showing the new values in both blocks, and a manual visual check confirming cards read as "popped above" the page in both light and dark mode.
- [x] 1.2 Add a code comment beside the `--card` and `--popover` definitions in `src/app/globals.css` stating they intentionally hold identical values per shadcn/ui's per-component-slot convention (card-popover-value-parity requirement). Verify by reading the comment in place and confirming `Popover` and `Card` render with identical resolved background colors in the same theme mode (visual check or computed-style inspection).

## 2. Remove redundant and dead tokens (no-redundant-background-tokens)

- [x] 2.1 Apply the design's "consolidate `--alt-mesh-bg` into `--background`" decision: delete `--alt-mesh-bg` from both `:root` and `.dark` in `src/app/globals.css`, and change `src/components/home/mesh-gradient.tsx`'s background `<rect fill="var(--alt-mesh-bg)">` to `fill="var(--background)"` — satisfying no-redundant-background-tokens (the mesh background resolves directly from the canonical token, with no alias in between, and renders visually identical to before since the values coincide). Verify with `grep -rn "alt-mesh-bg" src/` returning no matches, and a visual diff of the home hero showing no change in the rendered background color.
- [x] 2.2 Delete the dead tokens `--alt-canvas-soft`, `--alt-canvas-soft-2`, `--alt-ink`, and the entire `--sidebar`/`--sidebar-*`/`--color-sidebar-*` family (including their `@theme inline` mappings) from `src/app/globals.css`, with no change to any rendered surface (these tokens have zero consumers). Verify with `grep -rn "alt-canvas-soft\|alt-ink\|sidebar" src/app/globals.css` returning no matches, and `pnpm build` succeeding with no missing-variable warnings.

## 3. Component-level fixes

- [x] 3.1 [P] Apply the design's "CTA variant fix over color-system fix" decision by changing the home hero "View Portfolio" button in `src/components/home/cta-buttons.tsx` from `variant="destructive"` to `variant="default"`, so it renders with the theme-inverting `--primary` solid background instead of the red/error-colored destructive style, while the "About Me" `variant="outline"` button keeps its variant (its surface-token implementation is corrected separately in task 3.3). Verify by rendering the home page in both light and dark mode and confirming the "View Portfolio" button is no longer red/destructive-colored and inverts correctly between modes.
- [x] 3.2 [P] Remove the translucent `bg-background/50` wrapper `div` from `src/components/article/index.tsx` and from the About page content wrapper in `src/app/(site)/[lang]/about/[[...slug]]/page.tsx`, so article and About content renders directly against the page background with no overlay panel. Verify by rendering an article page and the About page in both modes and confirming there is no visible translucent panel behind the content (a visual diff should show no rendering change, since the overlay was already imperceptible).
- [x] 3.3 [P] Apply the design's "unify the outline button's surface token across modes" decision by replacing the `outline` variant's `bg-background hover:bg-accent ... dark:bg-input/30 dark:hover:bg-input/50` class string in `src/components/ui/button.tsx` with a single mode-independent `bg-background/50 hover:bg-accent ...` rule (no `dark:bg-input`/`dark:hover:bg-input` overrides), so every consumer (Dialog "Close", home "About Me" CTA, `GithubInfo` buttons, article info button) renders a translucent fill that blends with its surroundings identically in both themes. Verify with `grep -n "dark:bg-input\|dark:hover:bg-input" src/components/ui/button.tsx` returning no matches, and a visual check of at least one outline button in both light and dark mode confirming the fill is translucent and visually consistent (same blending behavior) across themes.

## 4. DESIGN.md "Paper & Ink" token renaming and documentation sync

- [x] 4.1 Apply the design's "paper & ink" naming theme for neutral/surface tokens decision by renaming DESIGN.md's neutral/surface brand-color table entries: `canvas`→`Chalk` (`#ffffff`), `canvas-soft`→`Paper` (`#fafafa`), `canvas-soft-2`→`Fog` (`#f5f5f5`), add a new `Soot` entry for `#080808`, `ink`→`Ink` (unchanged), `primary`→`Graphite`, `body`→`Slate`, `mute`→`Smoke`, `hairline`→`Hairline` (unchanged), `hairline-strong`→`Pewter`; leave `on-primary`/`selection-bg`/`selection-fg` as-is. Verify with `grep -n "canvas-soft\|canvas\b\|\bink\b" docs/DESIGN.md` showing no remaining references to the old positional names in the token table, and a content review confirming all 12 rows match the design's renaming table.
- [x] 4.2 Update DESIGN.md's CSS Variable Implementation mapping tables, Elevation & Depth table, Components catalog `backgroundColor` entries, and the terminal-mockup description (replacing the hard-coded `rgba(10,10,10,.68)` swatch description with one that derives from the `--background` token) to reflect the new two-surface-role model, the new "Paper & Ink" names, and the `--card`/`--popover` value-parity note — applying both the "Keep `--card` / `--popover` as separate-but-identical tokens" decision and the "dialog stays on `--background`, not `--card`" decision (Dialog's section must explicitly state it remains on `--background`). Verify by content review confirming no stale references to `canvas`/`canvas-soft`/`canvas-soft-2`/`alt-mesh-bg` remain anywhere in `docs/DESIGN.md` (`grep -n "canvas-soft\|alt-mesh-bg" docs/DESIGN.md` returns no matches) and that the Dialog/Popover sections explicitly state their token assignments and the `--card`/`--popover` parity.

## 5. Final verification

- [ ] 5.1 Run `pnpm type-check` and `pnpm build`, confirming both complete with no errors — establishing that the token renames and removals introduced no broken references anywhere in the codebase.
- [ ] 5.2 Perform a manual visual pass in both light and dark mode across: the home page (mesh-gradient background + both CTA buttons), a `Card`-bearing page (e.g. Projects or About), a `Dialog` (e.g. resume download dialog), a `Popover`, an `Empty` state (e.g. a not-found page), and at least one `outline`-variant button (e.g. Dialog "Close" or `GithubInfo`) — confirming card-family surfaces read as visually elevated relative to the page background in both modes (per card-elevation-contrast), the CTA button is no longer destructive-colored, the outline button's translucent fill blends consistently in both themes with no mode-specific token split, and no translucent wrapper panels remain. Record the result as the change's final acceptance check.
